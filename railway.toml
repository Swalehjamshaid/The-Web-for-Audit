[start]
# This command launches Gunicorn, using your 'app.py' file and 'app' instance name.
# It uses the shell form to ensure the $PORT variable is correctly read by the shell,
# which fixes the 'Error: ${PORT' is not a valid port number.' issue.
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
