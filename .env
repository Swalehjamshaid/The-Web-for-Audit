# ----------------------------------------------------
# 1. APPLICATION & CORE CONFIG
# ----------------------------------------------------

# CRITICAL: Flask Session Security Key
# MUST be a long, random string. Do NOT use the example value in production.
SECRET_KEY=y0ur_v3ry_l0ng_4nd_s3cur3_fl4sk_s3cr3t_k3y_h3r3

# Optional: Port for local testing. Railway automatically handles the deployed port.
PORT=8000

# ----------------------------------------------------
# 2. DATABASE CONFIG (PostgreSQL)
# ----------------------------------------------------

# CRITICAL: Connection string for your PostgreSQL service.
# Get this from your Railway PostgreSQL service's "Connect" tab.
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_DATABASE>

# ----------------------------------------------------
# 3. BACKGROUND TASK QUEUE (RQ/Redis)
# ----------------------------------------------------

# CRITICAL: Connection string for your Redis service (used for background tasks/queue).
# Get this from your Railway Redis service's "Connect" tab.
# Format: redis://host:port/0 or redis://user:password@host:port/0
REDIS_URL=redis://<REDIS_HOST>:<REDIS_PORT>/0

# ----------------------------------------------------
# 4. EMAIL CONFIG (Flask-Mail)
# ----------------------------------------------------

# Mail server host (e.g., smtp.gmail.com, smtp.sendgrid.net)
MAIL_SERVER=smtp.gmail.com

# Mail server port (587 for TLS, 465 for SSL)
MAIL_PORT=587

# This must be True, as per your app.py logic
MAIL_USE_TLS=True

# Login credentials for your SMTP server
MAIL_USERNAME=roy.jamshaid@gmail.com
MAIL_PASSWORD=<YOUR_SMTP_PASSWORD_OR_APP_PASSWORD>

# The email address that reports will be sent from
MAIL_DEFAULT_SENDER=roy.jamshaid@gmail.com
